/** Site shell (`72rem` + gutters). Class defined in `app/assets/css/main.css`. */
export const PUBLIC_SITE_SHELL_CLASS = 'site-shell'

/** Centered article reading column (`46rem`). Class defined in `app/assets/css/main.css`. */
export const PUBLIC_ARTICLE_READING_CLASS = 'article-reading-column'
