mutation Register($input: RegisterInput!) {
  register(input: $input) {
    token
    user {
      id
      username
      role
      displayName
      bio
      website
      avatarUrl
      createdAt
    }
  }
}
