mutation Login($input: LoginInput!) {
  login(input: $input) {
    token
    user {
      id
      username
      role
      displayName
      bio
      website
      avatarUrl
      createdAt
    }
  }
}
