# examples

- `calibrated/`: نمونه‌های bootstrap ساخته‌شده از voice profile، نه quote مستقیم
- `approved/`: فقط نمونه‌های صریحاً تأییدشده توسط علی
- `before-after/`: نمونه‌ی اصلاح همراه با reasoning
- `rejected/`: الگوهایی که نباید تکرار شوند

Agent نباید calibrated را «متن واقعی منتشرشده‌ی علی» معرفی کند.
